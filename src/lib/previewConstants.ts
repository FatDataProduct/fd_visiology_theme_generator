const CELL_W = 280;
const COLS = 4;
const GAP = 12;
const PADDING = 24;

/** Total SVG width of the dashboard preview at 100% scale */
export const PREVIEW_DASHBOARD_WIDTH = COLS * CELL_W + (COLS - 1) * GAP + PADDING;
